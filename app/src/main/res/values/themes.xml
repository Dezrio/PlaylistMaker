<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/text_settings</item>

        <item name="android:textColor">@color/text_settings</item>
        <item name="android:tint">@color/icon_settings</item>
        <item name="backgroundTint">@color/background_settings</item>
    </style>
</resources>